# Traefik Middleware: redirect www → non-www
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-www
  namespace: portfolio
spec:
  redirectRegex:
    regex: "^https://www\\.(.*)"
    replacement: "https://${1}"
    permanent: true
